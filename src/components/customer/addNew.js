import {
  Box,
  Button,
  Card,
  CardContent,
  TextField,
  InputAdornment,
  SvgIcon, Typography
} from '@mui/material';

export const newExporter = (props) => (
     <Typography
        sx={{ m: 1 }}
        variant="h4"
      >
        Exporters
      </Typography>

)